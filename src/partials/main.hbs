<script id="mainScript">
    getComponent(window.getConstants().MainComponent, function (MainComponent) {
        var me = document.getElementById('mainScript');
        var parentNode = me && me.parentNode;
        var module = '{{module}}';
        (new MainComponent()).init(parentNode, module);
    });
</script>